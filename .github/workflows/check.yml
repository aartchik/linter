name: Check
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Intsall deps
        run: go mod tidy
      -name: test
       run: go run test ./...